<p id="notice"><%= notice %></p>

<h1>Courses <%= Course.count %> </h1>


  <div class="card">
    <div class="">
      <% @courses.each do |course| %>
        <%= render 'courses/course', course: course %>
      <% end %>
    </div>
  </div>

<br/>

<br>

<%= link_to 'New Course', new_course_path %>
